
#include <ncurses.h>
#include <stdio.h>

// Initializing the cell structure
struct cell{
  int alive;
  int pos_x;
  int pos_y;
};

int main(){

  WINDOW *my_window;
  my_window = create_newwin(100,100,0,0);
  return 0;
}
  
  
